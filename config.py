import os

class Config:
    # === Пути ===
    # Укажите базовый путь к данным. Если папка называется '1th_test', поправьте здесь:
    BASE_DATA_DIR = '../data'
    
    # Активный тест (можно менять на '1st_test', '2nd_test', '3rd_test', '4th_test')
    # Если у вас папка '1th_test', укажите именно это имя:
    ACTIVE_TEST = '1st_test'  # или '1th_test'
    
    @property
    def DATA_DIR(self):
        return os.path.join(self.BASE_DATA_DIR, self.ACTIVE_TEST)

    # === Параметры сигнала ===
    FS = 20_000          # Частота дискретизации, Гц
    N_POINTS = 20_480    # Ожидаемая длина сигнала (точек)

    # === Параметры обработки ===
    BASELINE_FRAC = 0.15   # Доля файлов для базовой линии (здоровое состояние)
    N_BANDS = 50           # Количество частотных полос для спектральных признаков
    K_SIGMA = 3.0          # Множитель для порога (k * std)
    
    # Индекс колонки подшипника (0-based). 
    # Для 1st_test часто 0..7, для 2nd_test 0..3. 
    # Проверьте структуру ваших файлов.
    BEARING_COL = 0        

    # === Визуализация ===
    PLOT_STYLE = 'seaborn-v0_8-darkgrid'

# Глобальный экземпляр конфигурации
cfg = Config()